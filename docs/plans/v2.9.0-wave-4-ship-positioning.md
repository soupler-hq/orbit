---
id: v2.9.0-wave-4-ship-positioning
doc_type: plan
status: Active
version: v1
last_updated: 2026-04-05
scope: v2.9.0-wave-4
phase: Ship + Positioning
rank: 040
priority: P0
depends_on:
  - v2.9.0-wave-0-release-bootstrap
  - wave-0.5-foundation-hardening
blocks: []
---

# v2.9.0 Wave 4 — Ship + Positioning (Audit-Expanded)

> Milestone: v2.9.0 — Idea to Market
> Phase: Wave 4
> Planned: 2026-04-05
> Source: 360° Technical Audit (RHS 87.4 → target 90+)
> Status: Active — BLOCKED on Wave 0.5 Foundation Hardening

## Phase Goal

Close the v2.9.0 milestone. Ship the two original positioning items (CI modernization, README) plus four audit-sourced hardening gaps that block Golden Path classification.

All 7 issues must pass before v2.9.0 is tagged and Orbit is eligible for Golden Path promotion.

---

## Audit Context

A 360° technical audit on 2026-04-05 produced RHS **87.4 / 100 (Stable)**. Gap to Golden Path (90+): 2.6 points. Four findings block promotion:

| Finding | Severity | Issue |
|---------|----------|-------|
| 2 failing tests in `command-runtime.test.js` | HIGH | #205 |
| Config schema never validated at runtime | HIGH | #206 |
| `identity_separation: false` default | MEDIUM | #207 |
| Coverage measured on one file only | MEDIUM | #208 |
| Windows `C:\Users\` missing from PII scan | LOW | #211 |

Pre-existing Wave 4 items:

| Item | Issue |
|------|-------|
| CI modernization — Sentinel pipeline hardening | #79 |
| README positioning rewrite for idea-to-market | #81 |

---

## Wave Dependency Map

```
Wave 4A (parallel — independent file ownership)
├── #205  fix failing tests          [tests/command-runtime.test.js, bin/quick.js]
├── #206  config schema validation   [bin/orchestrator.js]
├── #207  identity_separation flip   [orbit.config.json, orbit.config.schema.json]
└── #208  expand coverage surface    [bin/safety-evaluator.js, bin/prompt-dispatch.js]

Wave 4B (parallel — #79 depends on 4A green; #81 is independent)
├── #211  Windows PII scan           [.github/workflows/orbit-sentinel.yml]
├── #79   CI modernization           [.github/workflows/orbit-sentinel.yml]
└── #81   README positioning         [README.md]

Wave 4C (sequential — integration and ship gate)
├── #80   registry consolidation     [orbit.registry.json]
├── eval  SOTA compliance run        [bin/eval.sh → ≥80% all axes]
├── docs  CHANGELOG v2.9.0 entry     [CHANGELOG.md]
└── ship  STATE.md finalization + PR
```

**Critical dependency**: Wave 4B CI (#79, #211) requires Wave 4A tests (#205) green first.
**Note**: README (#81) touches only docs — can parallelize with Wave 4A.

---

## Task Definitions

### Wave 4A — Code Quality & Security Hardening

#### #205 — Fix Failing Tests

```xml
<task type="test">
  <n>fix(tests): resolve 2 failing command-runtime assertions — PR isolation + branch-mismatch format</n>
  <files>
    tests/command-runtime.test.js
    bin/quick.js
    bin/checkpoint-manifest.js
  </files>
  <action>
    FAILURE 1: "quick runtime writes a checkpoint manifest" expects latest.metadata.pr === null
    but receives "#204" — live branch state leaks into manifest write when prStatus: 'not_open'
    is passed in args. Fix: when writeCheckpoint: true and prStatus === 'not_open', write
    pr: null directly without reading live git state. prStatus in args is authoritative.

    FAILURE 2: "quick runtime blocks when requested issue does not match branch" — gate fires
    correctly but assertion fails on output shape mismatch. Fix output shape to match
    documented contract in commands/commands.md, or fix stale assertion.

    Do NOT change any gate logic. Only fix isolation leak and format shape.
  </action>
  <verify>npm test -- --reporter=verbose → all 34 tests in file pass; npm test → 0 failing</verify>
  <done>npm test exits 0. Zero failures across full suite.</done>
</task>
```

#### #206 — Config Schema Validation at Startup

```xml
<task type="implement">
  <n>feat(security): enforce orbit.config.json schema validation at orchestrator startup</n>
  <files>
    bin/orchestrator.js
    docs/operations/error-codes.md
    tests/orchestrator.test.js
    package.json
  </files>
  <action>
    orbit.config.schema.json exists but is never validated against. Add validateConfig()
    private method to OrbitOrchestrator constructor using ajv (devDependency only).
    Throw ERR-ORBIT-007 with field-level message on schema violation.
    Emit WARN (no throw) when schema file is absent.
    Register ERR-ORBIT-007 in docs/operations/error-codes.md.
    Add one test: constructor throws ERR-ORBIT-007 on invalid config field type.
  </action>
  <verify>npm test → all pass; bad config field → throws ERR-ORBIT-007</verify>
  <done>Schema validated at every startup. ERR-ORBIT-007 documented. ajv in devDeps only.</done>
</task>
```

#### #207 — Flip identity_separation Default

```xml
<task type="configure">
  <n>fix(security): flip identity_separation default to true — enterprise-safe out-of-box</n>
  <files>
    orbit.config.json
    orbit.config.schema.json
    docs/architecture/security-model.md
    SECURITY.md
  </files>
  <action>
    Change "identity_separation": false → true in orbit.config.json.
    Update schema default to true.
    Add opt-out callout to security-model.md and SECURITY.md.
    No runtime logic changes — flag behavior already handles both values.
  </action>
  <verify>node -e "console.assert(require('./orbit.config.json').security.identity_separation === true)" → no error; npm test → 0 regressions</verify>
  <done>orbit.config.json ships identity_separation: true. Schema and docs updated.</done>
</task>
```

#### #208 — Expand Coverage Surface

```xml
<task type="test">
  <n>feat(test): expand vitest coverage surface to safety-evaluator and prompt-dispatch</n>
  <files>
    bin/safety-evaluator.js
    bin/prompt-dispatch.js
    vitest.config.js
    tests/safety-evaluator.test.js (new)
    tests/prompt-dispatch.test.js (new)
  </files>
  <action>
    Refactor bin/safety-evaluator.js: extract pure evaluation logic as named exports;
    wrap CLI stdin wiring in require.main === module guard.
    Verify bin/prompt-dispatch.js exports are already pure (used in command-runtime tests).
    Write tests/safety-evaluator.test.js (≥6 tests: clean input, rm -rf, injection, base64).
    Write tests/prompt-dispatch.test.js (≥4 tests: engineering/product/review classification).
    Update vitest.config.js include array to add both files.
    Remove sprint-3 deferral comment.
  </action>
  <verify>npm run coverage → both files in report; thresholds ≥60% met; npm test → 0 failures</verify>
  <done>Both files measured. ≥10 new tests passing. Sprint-3 deferral comment gone.</done>
</task>
```

---

### Wave 4B — CI + Docs

#### #211 — Windows PII Scan

```xml
<task type="configure">
  <n>fix(ci): add Windows C:\Users\ path pattern to Sentinel PII scan</n>
  <files>.github/workflows/orbit-sentinel.yml</files>
  <action>
    Extend lint job PII scan grep pattern to include C:\\Users\\ and C:/Users/ variants.
    Update step name to reflect cross-platform coverage.
  </action>
  <verify>actionlint → 0 errors; pattern rejects C:\Users\testuser test input</verify>
  <done>Windows paths caught. actionlint clean.</done>
</task>
```

#### #79 — CI Sentinel Modernization

See full spec in [GitHub issue #79](https://github.com/soupler-hq/orbit/issues/79).
Additional audit scope: timeout-minutes on all jobs, explicit npm ci guards before node calls.

#### #81 — README Positioning Rewrite

See full spec in [GitHub issue #81](https://github.com/soupler-hq/orbit/issues/81).
Additional audit scope: Codex column in comparison table, "What's New in v2.9.0" section, all 17 agents listed.

---

### Wave 4C — Integration Gate & Ship

```
npm test → 0 failures
npm run lint → 0 errors
npm run coverage → ≥60%, 3 files measured
node bin/orchestrator.js → validates config at startup, no throw
orbit.config.json → identity_separation: true
bash bin/eval.sh → all SOTA axes ≥80%
actionlint .github/workflows/orbit-sentinel.yml → 0 errors
CHANGELOG.md → v2.9.0 entry at top
orbit.registry.json → all v2.9.0 surfaces registered (#80)
README.md → 17 agents, Codex column, v2.9.0 section
RHS target → ≥90.0 (Golden Path)
```

---

## Definition of Done

| Gate | Target |
|------|--------|
| `npm test` | 0 failures (was: 2 failing) |
| `npm run lint` | 0 errors |
| `npm run coverage` | ≥60%, 3 files measured |
| Config validation | ERR-ORBIT-007 on bad config |
| `identity_separation` | defaults to true |
| `bash bin/eval.sh` | ≥80% all SOTA axes |
| `actionlint` sentinel | 0 errors |
| CHANGELOG | v2.9.0 entry present |
| Registry | all v2.9.0 surfaces registered |
| README | 17 agents, Codex column, v2.9.0 section |
| **RHS target** | **≥90.0 (Golden Path)** |

---

## Related Plans

| Plan | Relationship |
|------|-------------|
| [v2.9.0-wave-0-release-bootstrap.md](v2.9.0-wave-0-release-bootstrap.md) | Predecessor — Wave 0 established state infrastructure |
| [wave-0.5-foundation-hardening.md](wave-0.5-foundation-hardening.md) | Predecessor — Wave 0.5 must ship before Wave 4 can begin |
| [issue-130-orbit-enforcement-remediation.md](issue-130-orbit-enforcement-remediation.md) | Related — enforcement hardening feeds into Wave 4 governance gates |
